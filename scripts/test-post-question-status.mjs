import { readFileSync } from 'node:fs'
import assert from 'node:assert/strict'

const block = readFileSync(new URL('../src/components/question/PostQuestionBlock.vue', import.meta.url), 'utf8')
const detail = readFileSync(new URL('../src/views/PostDetailView.vue', import.meta.url), 'utf8')
const api = readFileSync(new URL('../src/api/question.ts', import.meta.url), 'utf8')

assert.match(detail, /<PostQuestionBlock :post-id="post\.postId" \/>/, 'post detail must render the knowledge structuring block for community content')
assert.match(detail, /知识资产路径/, 'post detail must show how a post becomes a reusable knowledge asset')
assert.match(detail, /从经验帖到可复用知识/, 'post detail must connect posts to long-term knowledge reuse')
assert.match(detail, /knowledgePathSteps = computed/, 'post detail must derive visible knowledge-asset path steps')
assert.match(detail, /primaryKnowledgeTopic = computed/, 'post detail must derive a topic for knowledge discovery links')
assert.match(detail, /AI 知识助手/, 'post detail must productize AI as a visible knowledge assistant')
assert.match(detail, /把这篇经验整理成可复用资产/, 'AI assistant must frame posts as reusable technical knowledge assets')
assert.match(detail, /aiKnowledgeInsights = computed/, 'post detail must derive AI knowledge insights from current content state')
assert.match(detail, /aiKnowledgeCompletenessScore = computed/, 'post detail must derive a visible knowledge quality score')
assert.match(detail, /highValueComment = computed/, 'post detail must surface comment Q&A extraction candidates')
assert.match(detail, /相似内容聚合/, 'post detail must expose similar-content aggregation as a community knowledge feature')
assert.match(detail, /个人复盘建议/, 'post detail must connect AI guidance to personal review space')
assert.match(detail, /path: '\/questions', query: knowledgeTopicQuery/, 'post detail must link from the post into knowledge cards')
assert.match(detail, /path: '\/search', query: knowledgeSearchQuery/, 'post detail must link from the post into similar community experience')
assert.match(detail, /to="\/me\/prep"/, 'post detail must link from the post into the personal review space')
assert.match(api, /taskStatus: 'none' \| 'pending' \| 'running' \| 'succeeded' \| 'failed'/, 'question API type must expose extraction task status')
assert.match(api, /extractedCount: number/, 'question API type must expose extracted question count')
assert.match(api, /pendingReviewCount: number/, 'question API type must expose pending review count')
assert.match(api, /reviewHint\?: string/, 'question API type must expose a review hint')
assert.match(api, /const questions = Array\.isArray\(raw\?\.questions\)/, 'post question block adapter must normalize questions before deriving counts')
assert.match(api, /raw\?\.extractedCount \?\? raw\?\.questionCount \?\? questions\.length/, 'post question block adapter must fall back to task questionCount')
assert.match(api, /Math\.max\(0, extractedCount - visibleCount\)/, 'post question block adapter must derive pending review count for older responses')
assert.match(block, /statusMeta/, 'PostQuestionBlock must derive a visible status label')
assert.match(block, /知识结构化中/, 'PostQuestionBlock must show an in-progress structuring state')
assert.match(block, /知识结构化失败/, 'PostQuestionBlock must show a failed structuring state to users')
assert.match(block, /结构化结果待审核/, 'PostQuestionBlock must explain successful structuring with pending review items')
assert.match(block, /pendingReviewCount/, 'PostQuestionBlock must render pending review count')
assert.match(block, /normalizedStatus\.value === 'succeeded' && extractedCount\.value > 0 && visibleCount\.value === 0/, 'PostQuestionBlock must treat succeeded extraction with zero visible questions as pending review')
assert.doesNotMatch(block, /[链鏆棰鍚绋]|�|\?\? \?\?\?\?/, 'PostQuestionBlock must not contain visible mojibake text')
assert.match(block, /setTimeout\(\(\) => \{[\s\S]*load\(\{ silent: true \}\)/, 'PostQuestionBlock must auto-refresh while extraction is pending or running')
assert.match(block, /onBeforeUnmount\(clearPoll\)/, 'PostQuestionBlock must clean up extraction polling timers')
assert.match(block, /重新结构化/, 'admins must keep a manual restructuring entry')

assert.match(api, /extractPostQuestions: \(postId: ApiId, remark: string\)/, 'manual extraction API must require an audit remark')
assert.match(api, /client\.post\(`\/api\/v1\/admin\/posts\/\$\{postId\}\/extract-questions`, \{ remark \}\)/, 'manual extraction API must send the audit remark to the backend')
assert.match(block, /window\.prompt/, 'manual extraction must ask the operator for a reason before submitting')
assert.match(block, /extractPostQuestions\(props\.postId, cleanRemark\)/, 'manual extraction must submit the cleaned audit remark')
assert.doesNotMatch(block, /extractPostQuestions\(props\.postId\)/, 'manual extraction must not submit without a remark')

console.log('post question status guard passed')
